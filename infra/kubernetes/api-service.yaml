apiVersion: v1
kind: Service
metadata:
  name: kota-pokedex-api
  labels:
    app: kota-pokedex-api
spec:
  type: ClusterIP
  selector:
    app: kota-pokedex-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
