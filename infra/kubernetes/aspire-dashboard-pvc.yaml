apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aspire-dashboard-data
  labels:
    app: aspire-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
